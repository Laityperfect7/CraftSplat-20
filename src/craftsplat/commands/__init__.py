"""
CraftSplat-20 命令行子模块
==========================

包含六个子命令的实现：
    capture_plan  — 生成拍摄角度表
    prepare       — 图片预处理
    colmap_cmd    — COLMAP wrapper
    train_cmd     — Nerfstudio wrapper
    export_cmd    — 模型导出
    report        — 运行报告
"""
